Project: Care_AI

Auto-detect module by path:

- /backend → backend
- /web_admin → frontend (HTML/CSS/JS)
- /mobile_app → mobile
- /ai_service → AI

Rules:
- Always follow module-specific rules
- Do not mix logic between modules